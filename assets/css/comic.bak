/* === Global styles === */
body {
  margin: 0;
  padding: 1rem;
  font-family: 'Roboto Mono', monospace, sans-serif;
  font-size: 1.1rem;
  color: #303030;
}

/* === Main content (mobile-first) === */
main {
  width: 100%;
  margin: 0 auto;
  padding: 0 0rem;
  line-height: 1.6;
  background-color: yellow;
}

/* Ensure images in Episode 1 stack without gaps */
main img {
    display: block;   /* removes inline spacing under images */
    margin: 0;        /* remove default margins */
    padding: 0;       /* remove default padding */
    width: 100%;      /* fill container width */
}

/* Optional: remove paragraph spacing before/after images */
main p {
    margin: 0 0 0.5rem 0;   /* small space after text, before first image */
}



/* === Media query adjustments === */
@media (min-width: 568px) {
  main {
    padding:   0 0; 
    background-color: red;
    max-width: 440px;
  }

}

