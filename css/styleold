/* Mobile first, clean width test */
.content-wrapper {

  background:blue;                 /* keep as a visual marker */
}
.nav {

  background:green;                 /* keep as a visual marker */
}
body {
  background:red;                 /* keep as a visual marker */
  margin: 0;
  padding: 1rem;                    /* safe spacing */
  font-family: 'Roboto Mono', monospace, sans-serif;
  font-size: 1.1rem;
  color:#404040;
}

/* === Blockquote styling === */
/* Blockquotes with clean vertical bar only on text lines */
blockquote {
  border-left: 2px solid #707070;   /* vertical bar */
  margin: 1.5em 0;
  padding-left: 1em;
  color: #444;
  font-style: italic;
}

blockquote p {
  margin: 0.5em 0;
}

blockquote p:empty {
  display: none; /* hide empty <p> so bar doesn't extend */
}


.ObjectList {
    text-decoration:none;
}
.site-nav {
    color:red;
}

.site-nav a {
    text-decoration: none;
    color: #404040;
}

.logo,.footnote {
    color:#404040;
}

h1,h2 {
    font-size:1.1rem;
    font-weight:normal;
    color:#404040;
    padding-top:40px;
}


/* Nav Bar styling */
.nav {
    padding-top:60px;
    padding-bottom:20px;
}

.nav a {
    text-decoration: none;
    color: #404040;
}

.nav  a:hover; {
    color:red ! important;
    font-weight:bold;
}

/* Object list styling */
.ObjectsList {
  list-style: none;        /* remove bullets */
  padding: 0;              /* reset default padding */
  margin: 0;               /* reset default margin */
}

.ObjectList li {
  margin-bottom: 1.5rem;   /* spacing between items (like double-spaced text) */
}

.ObjectList  a {
  color: #404040;          /* match your body text color */
  text-decoration: none;   /* remove underline */
}

.ObjectList a:hover {
  color: red;             /* black on hover */
}

/* Tablet/desktop */
@media (min-width: 768px) {
  body {
    max-width: 460px;               /* cap width only on bigger screens */
    margin: 0 auto;                 /* center it */
    background: ;          /* prove the media query kicks in */
  }
}

